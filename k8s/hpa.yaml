apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: container-service-hpa
  namespace: maersk
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: container-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70